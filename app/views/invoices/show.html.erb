<%= render partial: "shared/nav" %>

<body>
  <div class="row">
    <p class='col-12'>Invoice #<%= @invoice.id %></p>
  </div>

  <div id="current-invoice-status">
    <strong>Status: </strong><%= @invoice.status %>
  </div>
  <br>

  <p> Created on: <%= @invoice.created_at.strftime("%A, %B %-d, %Y") %></p>
  <p>Total Revenue: <%= @invoice.total_revenue_merchant(@merchant) %></p>
  <p>Discounted Revenue: <%= @invoice.discounted_revenue_merchant(@merchant) %></p>


  <h4>Customer:</h4>
    <%= @customer.first_name %> <%= @customer.last_name %><br>
  <br>
  <br>
  <h4>Items on this Invoice:</h4>
  <table class="table" id="invoice_table">
    <thead>
      <tr class="tr">
        <th class="th1">Item Name</th>
        <th class="th1">Quantity</th>
        <th class="th1">Unit Price</th>
        <th class="th1">Status</th>
        <th class="th1">Discount</th>
      </tr>
    </thead>

    <tbody>
      <% @invoice.invoice_items.each_with_index do |i, index| %>
        <section id="the-status-<%= i.id %>">
          <tr class="tr">
            <td style="text-align:center"><%= i.item.name %></td>
            <td style="text-align:center"><%= i.quantity %></td>
            <td style="text-align:center">$<%= i.unit_price %></td>
            <td style="text-align:center">
              <%= form_with model: @invoice, url: merchant_invoice_path(@merchant, @invoice), method: :patch, local: true do |f| %>
                <%= f.select :status, Invoice.statuses.keys, selected: "#{@invoice.status}" %>
                <%= f.submit 'Update Invoice' %>
              <% end %>
              </td>
            <td style="text-align:center" id="item_<%=index + 1%>">
              <% discount = i.applied_discount %>
              <% if discount %>
                <%= link_to "Bulk Discount", merchant_bulk_discount_path(@merchant, discount) %>
              <% else %>
                No Discount
              <% end %>
          </tr>
        </section>
      <% end %>
    </tbody>
  </table>

</body>
