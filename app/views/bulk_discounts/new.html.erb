<%= render partial: "shared/nav" %>

<%= render partial: 'form', locals: { model: @bulk_discount, url: merchant_bulk_discounts_path(@merchant), method: :post, class: 'merchant_bulk_discount_form', local: true}%>