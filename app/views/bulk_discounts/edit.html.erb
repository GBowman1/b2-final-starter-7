<%= render partial: "shared/nav" %>

<%= render partial: 'form', locals: { model: @bulk_discount, url: merchant_bulk_discount_path(@merchant, @bulk_discount), method: :patch, class: 'merchant_bulk_discount_form', local: true}%>